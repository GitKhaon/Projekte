def Forschung1(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wirtschaft',driver)
        ok(driver)

def Forschung2(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Militär',driver)
        ok(driver)

def Forschung3(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Militär',driver)
        ok(driver)

def Forschung4(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wissenschaft',driver)
        ok(driver)

def Forschung5(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wissenschaft',driver)
        ok(driver)

def Forschung6(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Militär',driver)
        ok(driver)

def Forschung7(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wirtschaft',driver)
        ok(driver)

def Forschung8(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wirtschaft',driver)
        ok(driver)

def Forschung9(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wissenschaft',driver)
        ok(driver)

def Forschung10(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wirtschaft',driver)
        ok(driver)

def Forschung11(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wissenschaft',driver)
        ok(driver)

def Forschung12(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wissenschaft',driver)
        ok(driver)

def Forschung13(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wissenschaft',driver)
        ok(driver)

def Forschung14(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wissenschaft',driver)
        ok(driver)

def Forschung15(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wissenschaft',driver)
        ok(driver)

def Forschung16(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wissenschaft',driver)
        ok(driver)

def Forschung17(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Seefahrt',driver)
        ok(driver)

def Forschung18(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Seefahrt',driver)
        ok(driver)

def Forschung19(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Seefahrt',driver)
        ok(driver)

def Forschung20(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wirtschaft',driver)
        ok(driver)

def Forschung21(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wirtschaft',driver)
        ok(driver)

def Forschung22(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wirtschaft',driver)
        ok(driver)

def Forschung23(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wirtschaft',driver)
        ok(driver)

def Forschung24(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wirtschaft',driver)
        ok(driver)

def Forschung25(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wissenschaft',driver)
        ok(driver)

def Forschung26(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wissenschaft',driver)
        ok(driver)

def Forschung27(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wirtschaft',driver)
        ok(driver)

def Forschung28(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wirtschaft',driver)
        ok(driver)

def Forschung29(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Wirtschaft',driver)
        ok(driver)
        
def Forschung30(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Militär',driver)
        ok(driver)

def Forschung31(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Militär',driver)
        ok(driver)

def Forschung32(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Militär',driver)
        ok(driver)

def Forschung33(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Militär',driver)
        ok(driver)
        
def Forschung34(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Seefahrt',driver)
        ok(driver)

def Forschung35(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Seefahrt',driver)
        ok(driver)

def Forschung36(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Seefahrt',driver)
        ok(driver)

def Forschung37(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Seefahrt',driver)
        ok(driver)

def Forschung38(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Seefahrt',driver)
        ok(driver)

def Forschung19(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Seefahrt',driver)
        ok(driver)

def Forschung39(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Seefahrt',driver)
        ok(driver)

def Forschung40(driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        Berater('f',driver)
        Tagesbelohnung(driver)
        ok(driver)
        Forschung('Seefahrt',driver)
        ok(driver)
