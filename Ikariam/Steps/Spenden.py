def Holz1(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (394//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz2(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (992//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz3(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (1732//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz4(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (2788//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz5(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (3783//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz6(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (5632//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz7(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (8139//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz8(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (10452//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz9(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (13298//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz10(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (18478//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz11(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (23213//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz12(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (29038//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz13(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (39494//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz14(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (49107//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz15(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (66010//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz16(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (81766//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz17(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (101146//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz18(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (134598//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz19(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (154304//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz20(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (205012//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz21(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (270839//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz22(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (311541//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz23(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (411229//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz24(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (506475//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz25(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (665201//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz26(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (767723//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz27(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (1007959//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz28(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (1240496//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz29(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (1526516//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz30(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (1995717//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz31(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (2311042//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz32(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (3020994//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Holz33(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Holz',driver)
        ok(driver)
        spendezahl = (3935195//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus1(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (1303//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus2(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (2689//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus3(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (4373//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus4(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (7421//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus5(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (10037//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus6(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (13333//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus7(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (20665//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus8(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (26849//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus9(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (37305//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus10(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (47879//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus11(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (65572//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus12(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (89127//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus13(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (106217//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus14(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (152739//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus15(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (193512//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus16(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (244886//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus17(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (309618//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus18(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (414190//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus19(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (552058//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus20(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (660106//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus21(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (925396//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus22(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (1108885//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus23(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (1471979//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus24(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (1855942//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus25(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (2339735//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus26(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (3096779//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus27(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (3903252//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus28(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (5153666//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)

def Luxus29(stadt,driverliste):
    for i in range(len(driverliste)):
        driver = driverliste[i]
        Tagesbelohnung(driver)
        ansicht('s',driver)
        Tagesbelohnung(driver)
        ok(driver)
        switch(stadt,driver)
        ok(driver)
        ansicht('i',driver)
        ok(driver)
        InselBonus('Luxus',driver)
        ok(driver)
        spendezahl = (6199765//len(driverliste)) + 1
        spenden('Holz',str(spendezahl),driver)
